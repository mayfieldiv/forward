{
  "name": "forward",
  "version": "1.0.0",
  "main": "out/app.js",
  "private": true,
  "repository": "https://github.com/mayfieldiv/forward.git",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --outfile=out/app.js"
  },
  "dependencies": {
    "toucan-js": "^2.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^2.2.2",
    "@cloudflare/wrangler": "^1.19.3",
    "esbuild": "^0.13.2"
  }
}
